# Modifications in the structure or schema of this file must be reflected
# in graphql/queueCrafting.js and events/handlers/crafting.js

tools:
  inputs:
    iron: 10
  outputs:
    tools: 1
  # all crafting takes time, like factorio
  duration: 2
  facility:
    type: account

factory:
  inputs:
    tools: 10
  outputs:
    factory: 1
  duration: 120
  facility:
    type: account